# Running short-form experiments

To run the preprocessing and inference script, set `CUDA_VISIBLE_DEVICES` appropriately, then run
```
pixi run python scripts/02-short/run.py
```
You can then run the inference notebook
